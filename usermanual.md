# 📖 Руководство пользователя: Автоматика отбора спирта на Arduino Nano (v3.0)

Полное руководство по эксплуатации, настройке экранов и подключению универсального блока управления соленоидным клапаном ректификационной колонны.

---

## 📌 Ключевые возможности

* **Гибкая логика отбора (3 режима):**
  * `MANUAL` — полностью ручное управление клапаном.
  * `AUTO` — классический «Старт-Стоп» по дельте температуры царги.
  * `PWM_AUTO` — «Старт-Стоп» с автоуменьшением скважности ШИМ (уменьшением скорости отбора) при каждом залёте.
* **Динамическая барокоррекция:**
  * Автоматическая корректировка целевой температуры при изменении атмосферного давления (коэффициент `0.037 °C / мм рт. ст.`).
  * Возможность ручной подстройки целевой температуры прямо с главного экрана «на лету».
* **Таймер стабилизации колонны:**
  * Настраиваемая пауза отстоя (от 1 до 15 минут) после закрытия клапана по залёту.
* **Бесшумный форсированный ШИМ:**
  * Частота ШИМ **31.25 кГц** на пине `D3` (никакого гула и писка катушки).
  * Двухрежимное открытие: мощный импульс (150 мс, 100% ШИМ) для надёжного срабатывания, затем переход в экономный режим удержания (по умолчанию ~15% мощности) для защиты от перегрева.
* **Безопасность и защита:**
  * Автоматическое отключение отбора при достижении уставки температуры в кубе ($T_{\text{stop}}$).
  * Аварийное отключение и звуковая сигнализация при перегреве корпуса автоматики ($\ge 50~^\circ\text{C}$).
* **Удобный 2-строчный интерфейс (LCD 1602 I2C):**
  * 11 настраиваемых экранов с сохранением всех параметров в энергонезависимую память EEPROM.
  * Интуитивная система навигации с поддержкой быстрого автоповтора при зажатии кнопок.

---

## 🕹️ Органы управления

Управление осуществляется 5 кнопками (подключение с внутренней подтяжкой `INPUT_PULLUP` к `GND`):

| Кнопка | Пин | В режиме просмотра (`VIEW_MODE`) | В режиме редактирования (`EDIT_MODE`) |
| :--- | :---: | :--- | :--- |
| **Вправо** | `A0` | Переход к следующему экрану (1 $\rightarrow$ 11) | Увеличение значения (Черновика) |
| **Вверх** | `A1` | Выбор элемента для редактирования (снизу вверх) | Переход к верхнему элементу / Выход |
| **Select** | `A2` | Вкл/Выкл клапана (только в `MANUAL` на Экране 1) | **Применить и сохранить** значение |
| **Вниз** | `A3` | Выбор элемента для редактирования (сверху вниз) | Переход к нижнему элементу / Выход |
| **Влево** | `D8` | Переход к предыдущему экрану (11 $\rightarrow$ 1) | Уменьшение значения (Черновика) |

> 💡 **Автоповтор:** При удержании кнопок **Влево/Вправо** более 0.5 сек в режиме редактирования значение начнёт меняться автоматически с высокой скоростью.

---

## 🖥️ Структура экранов меню и настройка

Всего в автоматике доступно **11 экранов**. Для входа в режим редактирования выберите нужный экран и нажмите **Вниз** или **Вверх**. Выбранный параметр начнёт мигать. Измените его кнопками **Влево/Вправо** и нажмите **Select** для сохранения.

### Экран 1: Главный командный экран
* **Элемент 1 (Захват базы):** Выберите элемент и нажмите **Select**. Произойдёт быстрый фиксаж текущих $T_{\text{base}}$ и $P_{\text{base}}$, а также сброс залётов.
* **Элемент 2 (Целевая T):** Позволяет вручную сместить целевую температуру залёта с шагом $0.01~^\circ\text{C}$ (автоматически пересчитает $T_{\text{base}}$).
* **Элемент 3 (Режим работы):** Переключение режимов `MANUAL` / `AUTO` / `PWM_AUTO`.

### Экран 2: Диагностика барометра
* Отображает текущее/базовое давление ($P / P_{\text{base}}$) и температуру внутри блока управления ($T_{\text{in}}$).

### Экраны 3 и 4: Фильтрация датчиков (EMA)
* **Стр. 3 (Weight A_T):** Вес EMA-фильтра Pt1000 ($1..50\%$, по умолч. $5\%$).
* **Стр. 4 (Weight A_P):** Вес EMA-фильтра давления BMP ($1..50\%$, по умолч. $3\%$).

### Экран 5: Порог залёта (Delta T)
* Задание допустимой дельты превышения температуры царги ($0.01..0.50~^\circ\text{C}$, по умолч. $0.10~^\circ\text{C}$).

### Экраны 6 и 7: Настройки ШИМ (для режима `PWM_AUTO`)
* **Стр. 6 (Start Duty):** Начальная скважность отбора ($10..100\%$, по умолч. $80\%$).
* **Стр. 7 (Decr Step):** Шаг уменьшения отбора при залёте ($1..25\%$, по умолч. $10\%$).

### Экран 8: Время отстоя (Stabilize Time)
* Пауза в минутах ($1..15$ мин, по умолч. $5$ мин) после восстановления температуры перед повторным открытием клапана.

### Экран 9: Энергосбережение соленоида (Hold PWM)
* Значение ШИМ удержания катушки после стартового импульса ($10..255$, по умолч. $40$ из 255 $\approx 15\%$ мощности).

### Экран 10: Отсечка куба (Cube Stop Temp)
* Температура в кубе для полного прекращения отбора ($80.0..100.0~^\circ\text{C}$, по умолч. $98.0~^\circ\text{C}$).

### Экран 11: Сброс на заводские настройки (Factory Reset)
* Выберите `YES` и нажмите **Select** для сброса всех параметров EEPROM к значениям по умолчанию.

---

## 💡 Светодиодная и звуковая индикация

* **Красный LED (`D5`):** Горит при залёте по температуре или Аварии перегрева корпуса.
* **Зеленый LED (`D6`):** Горит, когда активна автоматизация (`AUTO` или `PWM_AUTO`) при установленной базе.
* **Синий LED (`D7`):** Горит, когда соленоидный клапан физически открыт.
* **Зуммер (`D4`):**
  * Короткий щелчок — нажатие кнопки.
  * Двойной сигнал — успешное сохранение параметров / захват базы.
  * Низкий одиночный сигнал — отмена редактирования по таймауту.
  * Периодический писк (раз в минуту) — отсчёт времени в режиме стабилизации колонны.
  * Длинный тревожный сигнал — Авария (перегрев блока).

---

### 🛠️ Порядок работы на ректификации

1. **Прогрев колонны**  
   Включите автоматику. На стартовом экране LCD отобразится состояние датчиков (`DS: WAIT` сменится на `C: XX.X`).

2. **Работа «на себя»**  
   Переведите колонну в режим работы «на себя» для стабилизации разделения фракций.

3. **Захват базы**  
   Когда температура в царгах окончательно стабилизируется, зайдите на **Экран 1**, выберите первый элемент и нажмите **Select**.  
   * Дисплей отобразит надпись `C OK`, зафиксировав эталонные значения $T_{\text{base}}$ и $P_{\text{base}}$.

4. **Выбор режима отбора**  
   * **`AUTO`** — выберите для отбора голов или работы в режиме классического Старт-Стопа.
   * **`PWM_AUTO`** — выберите для автоматизированного отбора тела с саморегулированием (постепенным снижением) скорости отбора.

5. **Завершение процесса**  
   При достижении в кубе температуры $T_{\text{stop}}$ (например, $98.0~^\circ\text{C}$) автоматика автоматически переключит режим в `MANUAL` и перекроет клапан отбора.